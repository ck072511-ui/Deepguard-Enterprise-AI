"""
DeepGuard — Core domain layer.

This package contains the pure business logic with zero infrastructure
dependencies. Follows Clean Architecture: entities, value objects,
use cases, and interfaces live here.

Packages:
    core.domain          — Domain entities and value objects
    core.use_cases       — Application business rules (interactors)
    core.interfaces      — Abstract repository and service interfaces
    core.exceptions      — Domain-level exception hierarchy
"""
