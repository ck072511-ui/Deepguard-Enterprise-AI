"""
DeepGuard — Utilities package.

Shared utility modules used across all layers of the application.

Packages:
    utils.image         — Image loading, resizing, normalization helpers
    utils.video         — Video frame extraction and processing
    utils.metrics       — Classification metric computation (AUC, F1, etc.)
    utils.visualization — Attention heatmaps, GradCAM, result plotting
    utils.io            — File I/O, path management, serialization
    utils.logging       — Structured logger factory and configuration
"""
